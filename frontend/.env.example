# Optional: API base URL for production. No trailing slash.
# Leave unset in local dev (Vite proxy will forward /api to the backend).
# Set in Vercel to your deployed backend URL, e.g. https://your-backend.railway.app
# VITE_API_ORIGIN=https://your-backend.railway.app
